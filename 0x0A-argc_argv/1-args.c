#include <stdio.h>
#include "main.h"
/**
 * main- entry point
 * @argc: argument count
 * @argv: is a vector of C string
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d", argc -1);
		return (0);
}
