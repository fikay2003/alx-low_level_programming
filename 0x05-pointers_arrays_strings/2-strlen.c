#include "main.h"
/**
* _strlen - is the function that returns the length of a string
* @*s: char input
* Return: always 0
*/
int _strlen(char *s)
{
char s1[12] = "My first strlen!";
int  len;
len = strlen(s1);
}
