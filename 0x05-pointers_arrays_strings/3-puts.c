#include "main.h"
/**
 * main- entry point
 * _puts: function used to print string
 * @str: pointer to string
 * return: always 0
 */
void _puts(char *str);
char str[20];
_putchar(str);
_putchar("\n");
return (0);
}
