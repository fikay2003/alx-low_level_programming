#include <stdio.h>
/**
* main-Entry point
*
* return:Always 0 (Success)
*/
int main(void)
{
	for (char ch = 'a'; ch <= 'z'; ++ch)
		putchar(ch);
putchar('\n');
	for (char ch = 'A'; ch <= 'Z'; ++ch)
		putchar(ch);
return (0);
}

