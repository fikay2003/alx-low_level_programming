#include <stdlib.h>
#include <time.h>
/**
* Main - assings a random number to variable x
*	then  print the last digit of the number stored in the variable n
*
* Return - Always 0
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 5) = a
printf("%s Last digit of %d n is a") 
	return (0);
}
